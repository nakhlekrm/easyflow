import React from "react";
import Button from "../../components/button";
import { theme } from "../../theme/theme";
import './why.css';
const Why = ( ) => {
  return (
    <div isRow style={{textAlign:'center', justifyContent: "space-between", maxWidth:500,}}>
    <h1>Why hire your developers through Proxify?</h1
  >
  <h3>Transparent pricing, earlier start dates, and trusted developers. 
    Avoid rip-off recruitment agencies placing untested candidates.</h3>
    </div>
   
  )
}

export default Why;